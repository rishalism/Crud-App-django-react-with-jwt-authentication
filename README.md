# Crud-App-django-react-with-jwt-authentication
A full-stack task manager built with Django REST Framework and React (Vite). Includes JWT authentication and basic CRUD operations with a clean API-driven architecture.
